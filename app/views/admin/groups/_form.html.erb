<%= form_for(@admin_group) do |f| %>
  <% if @admin_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_group.errors.count, "error") %> prohibited this admin_group from being saved:</h2>

      <ul>
      <% @admin_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :index %><br />
    <%= f.text_field :index %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
